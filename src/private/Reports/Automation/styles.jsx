import { StyleSheet } from 'react-native';

const AutomationItemStyles = StyleSheet.create({
	subtitleView: { marginTop: 5, flexDirection: 'row' },
	subtitle: { color: 'grey', fontSize: 10 },
});

const AutomationReportStyles = StyleSheet.create({
	automations: { width: '95%' },
});

export { AutomationItemStyles, AutomationReportStyles };
